# Copie para .env e preencha com valores reais antes de subir o
# docker-compose.yml. NUNCA versionar o .env.

# Banco (metas_db)
DB_PASSWORD=troque_esta_senha

# Backend
SECRET_KEY=gere_com_openssl_rand_hex_32
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Portas de host — A CONFIRMAR com o usuário antes do deploy.
BACKEND_PORT=8002
FRONTEND_PORT=8090
